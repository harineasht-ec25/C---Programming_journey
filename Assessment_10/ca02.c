// Program to get a single number and print its ASCII value
#include <stdio.h>

int main()
{
    char num;

    // Read a single digit (as character, stored in int)
    scanf("%c", &num);

    // Print ASCII value
    printf("%d", num);

    return 0;
}
