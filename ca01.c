// Program to get an alphabet and print its ASCII value
#include <stdio.h>

int main()
{
    char ch;

    // Read an alphabet (stored as int)
    scanf("%c", &ch);

    // Print ASCII value
    printf("%d", ch);

    return 0;
}
